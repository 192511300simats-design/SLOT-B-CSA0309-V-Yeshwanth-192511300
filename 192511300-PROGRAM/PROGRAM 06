#include <stdio.h>
int main() 
{
    int n, i;
    if (scanf("%d", &n) != 1) return 0;
    int a[n];
    for (i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }
    if (n >= 5) {
        printf("%d\n", a[4]);
    }
    return 0;
}

output:
5th element=50
